const router = require('express').Router();
const {} = require('../models');

//GET all blog posts for homepage
router.get('/', async (req, res) => {
    try {
        const blogPostData = await
    }
})