<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>

<body class="container-fluid">
    <section class="flex-row justify-center">
      {{!-- Home page shows title and login button
      If user logs in and session is not over, login button turns to logout button --}}
    {{!-- <a href="/">Home | </a> --}}
    {{#if loggedIn}}
    <div class="w-100">
      <div class="row">
        <header>
          <h1>Welcome, {{user.username}}!</h1>
        </header>
      </div>
      <div>
        <a href="/dashboard">My Dashboard</a>
        <button class="no-button" id="logout" class="btn-no-style">Logout</button>
      </div>
    </div>
    {{else}}
    <div class="w-100">
      <div class="row">
        <header>
          <h1>Tech Blog</h1>
        </header>
      </div>
    </div>
    <div>
      <div class="row text-center my-2">
        <a class="my-login" href="/login"> Login</a>
      </div>
    </div>
    {{/if}}
  {{!-- </nav> --}}
  </section>
  <main>
    <div class="goal">
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>